import React from 'react'
import { divWithChildrenMock } from '../../../../mocksUtils'

const CollectionOwnerCheckMock = (children: React.ReactNode) =>
  divWithChildrenMock(children, 'CollectionOwnerCheck')

export default CollectionOwnerCheckMock
